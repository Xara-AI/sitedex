// Package export emits an indexed site's knowledge base as markdown files
// or JSONL, for the "sitedex export" CLI command.
//
// Target milestone: M2 (markdown), M3 (JSONL). See CLAUDE.md, "Command
// Surface (CLI)".
package export
